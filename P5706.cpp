#include <bits/stdc++.h>

int main() {
    double a;
    unsigned int b;
    std::cin >> a >> b;

    std::cout << std::setprecision(3) << std::fixed << a / b << std::endl
              << b * 2 << std::endl;

    return 0;
}